import styled from "@emotion/styled";

export const SButton = styled.button<{ contained: boolean }>`
  background-color: var(--black);
  border: none;
  padding: 0.5rem 2rem;
  border-radius: 0.3rem;
  font-weight: bold;
`;
